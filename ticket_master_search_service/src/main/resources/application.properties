spring.application.name=ticket_master_search_service

server.port=8081

event.main.service.base.url = http://127.0.0.1:8080/api/v1

# Elasticsearch auto-configuration for Spring Data Elasticsearch
# The Docker container exposes HTTP on port 9200 with security disabled.
spring.elasticsearch.uris=http://127.0.0.1:9200